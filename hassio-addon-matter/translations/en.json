{
    "configuration": {
        "log_level": {
            "name": "Log Level",
            "description": "Set the logging level. (Default: INFO) Setting to DEBUG or TRACE may cause performance degradation."
        },
        "use_supervisor_mqtt": {
            "name": "Use Home Assistant MQTT Integration",
            "description": "Connect to the MQTT broker through Home Assistant's MQTT integration (enables auto-login). If enabled, the MQTT credentials below will be ignored. If disabled, you must enter the MQTT URL, username, and password."
        },
        "mqtt_url": {
            "name": "MQTT URL",
            "description": "URL of the MQTT broker."
        },
        "mqtt_need_login": {
            "name": "MQTT Require Login",
            "description": "Enable if the MQTT broker requires authentication. Set to true if using the mosquitto addon."
        },
        "mqtt_user": {
            "name": "MQTT Username",
            "description": "Username for MQTT broker authentication."
        },
        "mqtt_passwd": {
            "name": "MQTT Password",
            "description": "Password for MQTT broker authentication."
        },
        "mqtt_topic_prefix": {
            "name": "MQTT Topic Prefix",
            "description": "Prefix for MQTT topics (default: homenet2mqtt). No need to change unless in special circumstances."
        },
        "discovery_enabled":{
            "name": "Discovery Enable",
            "description": "Enable Home Assistant discovery function (automatic device/entity registration). (Default: true)"
        },
        "config_files": {
            "name": "Config Files",
            "description": "List of configuration files to load. If left empty, the default (default.homenet_bridge.yaml) will be loaded."
        },
        "timezone": {
            "name": "Timezone",
            "description": "System timezone (e.g., Asia/Seoul). If left empty, the server uses UTC timezone, and the UI uses the browser's timezone."
        }
    }
}