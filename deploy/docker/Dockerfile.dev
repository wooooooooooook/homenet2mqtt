FROM node:22-slim

# Install system dependencies
RUN apt-get update && apt-get install -y \
    socat \
    procps \
    git \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /workspace

# Corepack enable is needed for pnpm
RUN corepack enable

# Copy source code
COPY . /workspace
